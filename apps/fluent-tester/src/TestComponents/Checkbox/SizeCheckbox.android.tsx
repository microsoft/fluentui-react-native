import * as React from 'react';
import { Text } from '@fluentui-react-native/experimental-text';

const style = { margin: 10, fontSize: 14 };
export const SizeCheckbox = () => {
  return (
    <Text style={style} color="red">
      Unsupported on Android
    </Text>
  );
};
