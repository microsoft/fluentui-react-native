import React from 'react';
import { AppRegistry, View } from 'react-native';

function BundleSizeApp() {
  return React.createElement(View);
}

AppRegistry.registerComponent('BundleSize', () => BundleSizeApp);
