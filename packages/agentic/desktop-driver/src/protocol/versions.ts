/** Version of the desktop host channel protocol and persisted run artifacts. */
export const DESKTOP_PROTOCOL_VERSION = 1;

/** Version of the serializable story-test plan schema. */
export const STORY_PLAN_SCHEMA_VERSION = 1;

/** Version of the portable WebdriverIO command matrix. */
export const PORTABLE_COMMAND_MATRIX_VERSION = 2;
