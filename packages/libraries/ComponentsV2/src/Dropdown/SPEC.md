# Dropdown native specification

`Dropdown` is a non-editable listbox trigger with controlled and uncontrolled open and selection state, single and multiple selection, clearability, disabled options, appearances, and sizes. Native menu roles replace browser listbox DOM semantics.
