# Divider native specification

`Divider` maps the Fluent UI Web horizontal and vertical orientations, four appearances, inset spacing, content alignment, and custom container styles to native views. React Native does not expose a separator role, so the native view uses a bounded accessibility label.
