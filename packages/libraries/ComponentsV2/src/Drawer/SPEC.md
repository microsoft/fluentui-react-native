# Drawer native specification

`Drawer` supports controlled and uncontrolled state, inline and overlay presentation, start and end positions, Fluent sizes plus custom widths, separators, modal backdrop dismissal, prevented close behavior, titles, and arbitrary body content. DOM persistence and CSS motion settings are represented as stable native rendering rather than browser portal behavior.
