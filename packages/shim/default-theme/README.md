# @fluentui-react-native/default-theme

This package is a compatibility shim. Default theme construction now lives in
`@fluentui-react-native/design` and is exposed through
`@fluentui-react-native/design/theming/compat/defaults`.

Existing imports continue to work. New code should import from the design
subpath directly.
