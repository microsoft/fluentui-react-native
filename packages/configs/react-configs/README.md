# `@fluentui-react-native/react-configs`

Common babel and jest configuration for pure react package in the fluentui-react-native repository.

This is a separate package to avoid the peer requirements from the react-native versions.
