import * as React from 'react';

import type { OverflowProps } from './Overflow.types';

export function useOverflow(_props: OverflowProps) {
  React.useLayoutEffect(() => null, []);
  return null;
}
