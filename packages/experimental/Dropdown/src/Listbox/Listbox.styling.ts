import { UseStylingOptions } from '@fluentui-react-native/framework';
import { listboxName, ListboxProps, ListboxSlotProps, ListboxTokens } from './Listbox.types';

export const stylingSettings: UseStylingOptions<ListboxProps, ListboxSlotProps, ListboxTokens> = {
  tokens: [listboxName],
};
