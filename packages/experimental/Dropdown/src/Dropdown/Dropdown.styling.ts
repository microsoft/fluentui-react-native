import { UseStylingOptions } from '@fluentui-react-native/framework';
import { dropdownName, DropdownProps, DropdownSlotProps, DropdownTokens } from './Dropdown.types';

export const stylingSettings: UseStylingOptions<DropdownProps, DropdownSlotProps, DropdownTokens> = {
  tokens: [dropdownName],
};
