# Build pipeline for publishing

trigger:
  batch: true
  branches:
    include:
      - master

pr: none

variables:
  - group: 'NPM and Github Secrets'

jobs:
  - template: templates/jobs/publish-npm-packages-job.yml
  - template: templates/jobs/publish-nuget-package-job.yml
